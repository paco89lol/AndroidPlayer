package com.example.testvideo.PPlayer;

public class PlayerViewGesturePreference {
    float PROGRESS_SCROLL_DRATION = 2f;// 设定进度滑动时的步长，避免每次滑动都改变，导致改变过快
    float VOLUME_SCROLL_DRATION = 2f;// 协调音量滑动时的步长，避免每次滑动都改变，导致改变过快
    float BRIGHTNESS_SCROLL_DRATION = 2f;// 协调音量滑动时的步长，避免每次滑动都改变，导致改变过快
}
